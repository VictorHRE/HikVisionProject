/*
  Historial: comentado para ocultarlo por completo (por si se vuelve a ocupar en el futuro).
  Se conserva el archivo para referencia y restauración fácil.

  Si quieres volver a habilitarlo remueve estos comentarios y restáuralo en la ruta
  desde `App.tsx` y `Sidebar.tsx`.
*/

export default function HistoryPage() {
  // Historial deshabilitado: devolvemos null para que la app siga compilando,
  // y la ruta en `App.tsx` y el enlace en `Sidebar.tsx` deben ser comentados.
  return null;
}
